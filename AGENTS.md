# Repository instructions

For repository structure, implementation conventions, validation and testing requirements, follow `.github/copilot-instructions.md`.

In particular, tests should exercise real feature behaviour. Presence checks such as executable existence or `--version` are smoke checks only and are not sufficient acceptance coverage for behaviour changes.
