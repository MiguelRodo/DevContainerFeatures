{
    "name": "renv cache",
    "id": "renv-cache",
    "version": "1.10.0",
    "description": "Configure R with renv cache",
    "postCreateCommand": "/usr/local/bin/renv-cache-post-create",
    "options": {
        "repositories": {
            "type": "string",
            "description": "Comma-separated list of GitHub repos whose `renv.lock` files it will restore, e.g. `myUsername/repo1,myUsername/repo2`. By default, uses the `renv.lock` file at the root of the repository. Can specify branch (e.g., `myUsername/repo1@branch`), `renv` profile (e.g., `myUsername/repo1:renvProfile`) or both (e.g., `myUsername/repo1@branch:renvProfile`)",
            "default": ""
        },
        "pkg": {
            "type": "string",
            "description": "Comma-separated list of specific packages to cache explicitly.",
            "default": ""
        },
        "pkgExclude": {
            "type": "string",
            "description": "Comma-separated list of packages to exclude from the renv snapshot restore process.",
            "default": ""
        },
        "restore": {
            "type": "boolean",
            "description": "Whether to run package restoration using renvvv::renvvv_restore(). Default is true.",
            "default": true
        },
        "update": {
            "type": "boolean",
            "description": "Whether to run package update using renvvv::renvvv_update(). If both restore and update are true, renvvv::renvvv_restore_and_update() is used. Default is false.",
            "default": false
        },
        "createUnifiedLockfile": {
            "type": "string",
            "enum": [
                "auto",
                "true",
                "false"
            ],
            "description": "Whether to create a single unified `renv.lock` file combining dependencies. 'auto' creates it only if multiple lockfiles or explicit packages are provided. 'true' always creates it. 'false' skips creation.",
            "default": "auto"
        },
        "purgePostUnification": {
            "type": "boolean",
            "description": "If true, removes all packages from the global cache that are not present in the final unified lockfile, if created. Useful for saving disk space, but may mean specific package versions for specific lock files may not be available. Note that if `update` is true, then the purge is performed to retain the packages and versions in the updated unified lockfile, rather than the original (restore-based) unified lockfile. Defaults to false.",
            "default": false
        },
        "setRLibPaths": {
            "type": "boolean",
            "description": "Whether to set default paths for R libraries (including for `renv`) to avoid needing to reinstall upon codespace rebuild.",
            "default": true
        },
        "overrideTokensAtInstall": {
            "description": "If true, temporarily override GITHUB_TOKEN and set GITHUB_PAT from the best available token (priority: GITHUB_PAT > GH_TOKEN > GITHUB_TOKEN) during the renv package install phase. Tokens are reset to their original values after install completes. Disable if you want to manage tokens manually.",
            "type": "boolean",
            "default": true
        },
        "renvDir": {
            "type": "string",
            "description": "Path to the directory containing subdirectories with `renv.lock` files. Defaults to `/usr/local/share/renv-cache/renv` if the environment variable is not set.",
            "default": "/usr/local/share/renv-cache/renv"
        },
        "usePak": {
            "type": "boolean",
            "description": "Whether to use `pak` for package installation. For some reason, restoring from the `renv` cache has not worked when using `pak` to build the image, so this is not recommended.",
            "default": false
        },
        "debugRenv": {
            "type": "boolean",
            "description": "Whether to use verbose renv output. Sets `RENV_CONFIG_INSTALL_VERBOSE` and `RENV_VERBOSE` to true when enabled.",
            "default": false
        },
        "installSystemRequirements": {
            "type": "boolean",
            "description": "Uses the Posit API to install apt-dependencies.",
            "default": true
        },
        "cranMirror": {
            "type": "string",
            "default": "https://cloud.r-project.org"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils",
        "ghcr.io/rocker-org/devcontainer-features/apt-packages",
        "ghcr.io/rocker-org/devcontainer-features/r-apt",
        "ghcr.io/rocker-org/devcontainer-features/r-rig"
    ]
}