{
    "name": "MiguelRodo Utils",
    "id": "utils",
    "version": "1.2.0",
    "description": "Installs Miguel Rodo's utilities like 'repos' and 'setupmjr'.",
    "options": {
        "installRepos": {
            "type": "boolean",
            "default": true,
            "description": "Install the 'repos' utility."
        },
        "reposVersion": {
            "type": "string",
            "default": "2.7.1",
            "description": "Version of 'repos' to install."
        },
        "installSetupmjr": {
            "type": "boolean",
            "default": true,
            "description": "Install the 'setupmjr' utility."
        },
        "setupmjrVersion": {
            "type": "string",
            "default": "0.7.5",
            "description": "Version of 'setupmjr' to install."
        },
        "runOnStart": {
            "type": "boolean",
            "default": false,
            "description": "Automatically run 'repos clone' when the container starts (only applies if installRepos is true)."
        }
    },
    "postStartCommand": "/usr/local/bin/utils-post-start"
}
