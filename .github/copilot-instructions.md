# Copilot instructions

Follow the repository instructions in `../AGENTS.md`. They are the canonical source for repository structure, implementation conventions, validation, testing, dependency handling, pull requests and release behaviour.

Copilot-specific environment setup is maintained separately in `.github/workflows/copilot-setup-steps.yml`.
